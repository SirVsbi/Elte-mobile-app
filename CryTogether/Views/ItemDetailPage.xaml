<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:model="clr-namespace:CryTogether.Models"
             x:Class="CryTogether.Views.ItemDetailPage"
             Title="{Binding Title}">

    <StackLayout Spacing="20" Padding="15" x:DataType="model:Breakdown">
        <Label Text="Text:" FontSize="Medium" TextColor="Black"/>
        <Label Text="{Binding Name}" FontSize="Small" TextColor="Black"/>
        <Label Text="Description:" FontSize="Medium" TextColor="Black"/>
        <Label Text="{Binding Description}" FontSize="Small" TextColor="Black"/>
        <Label Text="{Binding suferer}" TextColor="Black"/>
    </StackLayout>
    
</ContentPage>